@model MigrationTool.Models.XenAppFarmVersion
@using MigrationTool.ViewModels

@{
    ViewBag.Title = "Edit";
}

@Html.Partial("_Breadcrumb", new Breadcrumb(ViewBag.Title, new BreadcrumbItem { LinkText = "XenApp Farm Versions", ControllerName = "XenAppFarmVersion" }))
<h2>@ViewBag.Title</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    @Html.HiddenFor(model => model.Id)

    <div class="form-group">
        @Html.LabelFor(model => model.Name)
        @Html.EditorFor(model => model.Name)
        @Html.ValidationMessageFor(model => model.Name)
    </div>
    
    <div class="form-group">
        @Html.EditorFor(model => model.Billable)
        @Html.ValidationMessageFor(model => model.Billable)
    </div>
        
    <button type="submit" class="btn btn-primary">Save</button>
    @Html.ActionLink("Cancel", "Details", new { Id = Model.Id }, new { @class = "btn btn-default" });
    if (User.IsInRole(MigrationTool.Roles.Admin))
    {
        <div class="pull-right">
            @Html.ActionLink("Delete", "Delete", new { Id = Model.Id }, new { @class = "btn btn-danger" })
        </div>
    }                                                                                                            
}