@model IEnumerable<MigrationTool.Models.HypervisorGroupType>
@using MigrationTool.ViewModels

@{
    ViewBag.Title = "Index";
}

@Html.Partial("_Breadcrumb", new Breadcrumb("Hypervisor Group Types"))

@using (Ajax.BeginForm("FilterIndex", "HypervisorGroupType", new AjaxOptions {
    InsertionMode = InsertionMode.Replace,
    UpdateTargetId = "HypervisorGroupTypeList",
    LoadingElementId = "HypervisorGroupTypeLoader",
    OnBegin = "$(\"#HypervisorGroupTypeLoader .spinner\").spin();",
    OnComplete = "$(\"#HypervisorGroupTypeLoader .spinner\").spin(false);"
})) {
    @Html.Partial("_StandardFilterNoActive")
}


<p></p>
@if (User.IsInRole(MigrationTool.Roles.Edit) || User.IsInRole(MigrationTool.Roles.Admin))
{
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-default" })
    <p></p>
}
@Html.Partial("_Loader", "HypervisorGroupTypeLoader")
<div id="HypervisorGroupTypeList">
    @Html.Partial("_List", Model)
</div>