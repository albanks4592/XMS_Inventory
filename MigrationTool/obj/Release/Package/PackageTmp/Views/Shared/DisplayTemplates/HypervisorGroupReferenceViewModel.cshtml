@model MigrationTool.ViewModels.HypervisorGroupReferenceViewModel
@if (Model != null)
{
    if (Model.Inactive)
    {
        <s>@Html.ActionLink(Model.Name, "Details", "HypervisorGroup", new { Id = Model.Id }, null)</s>
    }
    else
    {
        @Html.ActionLink(Model.Name, "Details", "HypervisorGroup", new { Id = Model.Id }, null)
    }
}