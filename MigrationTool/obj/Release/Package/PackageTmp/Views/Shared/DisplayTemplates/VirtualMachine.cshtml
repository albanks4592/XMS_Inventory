@model MigrationTool.Models.VirtualMachine
@if (Model != null)
{
    if (Model.Inactive)
    {
        <s>@Html.ActionLink(Model.Name, "Details", "VirtualMachine", new { Id = Model.Id }, null)</s>
    }
    else
    {
        @Html.ActionLink(Model.Name, "Details", "VirtualMachine", new { Id = Model.Id }, null)
    }
}