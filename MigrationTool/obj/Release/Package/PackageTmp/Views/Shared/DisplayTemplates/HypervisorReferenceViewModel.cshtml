@model MigrationTool.ViewModels.HypervisorReferenceViewModel
@if (Model != null)
{
    if (Model.Inactive)
    {
        <s>@Html.ActionLink(Model.Name, "Details", "Hypervisor", new { Id = Model.Id }, null)</s>
    }
    else
    {
        @Html.ActionLink(Model.Name, "Details", "Hypervisor", new { Id = Model.Id }, null)
    }
}