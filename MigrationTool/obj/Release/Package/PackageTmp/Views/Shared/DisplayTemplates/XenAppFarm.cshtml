@model MigrationTool.Models.XenAppFarm
@if (Model != null)
{
    if (Model.Inactive)
    {
        <s>@Html.ActionLink(Model.Name, "Details", "Farm", new { id = Model.Id }, null)</s>
    }
    else
    {
        @Html.ActionLink(Model.Name, "Details", "Farm", new { id = Model.Id }, null)
    }
}