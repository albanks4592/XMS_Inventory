@model MigrationTool.Models.ActiveDirectoryGroup
@if (Model != null)
{
    if (Model.Inactive)
    {
        <s>@Html.ActionLink(Model.Name, "Details", "ActiveDirectoryGroup", new { Id = Model.Id }, null)</s>
    }
    else
    {
        @Html.ActionLink(Model.Name, "Details", "ActiveDirectoryGroup", new { Id = Model.Id }, null)
    }
}