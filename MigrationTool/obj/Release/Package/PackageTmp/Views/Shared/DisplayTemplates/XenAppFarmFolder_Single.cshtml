@model MigrationTool.Models.XenAppFarmFolder
@if (Model != null)
{
    if (Model.Inactive)
    {
        <s>@Html.ActionLink(Model.Name, "Details", "XenAppFarmFolder", new { Id = Model.Id }, null)</s>
    }
    else
    {
        @Html.ActionLink(Model.Name, "Details", "XenAppFarmFolder", new { Id = Model.Id }, null)
    }
}