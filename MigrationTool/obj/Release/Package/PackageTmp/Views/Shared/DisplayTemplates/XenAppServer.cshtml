@model MigrationTool.Models.XenAppServer
@if (Model != null)
{
    if (Model.Inactive)
    {
        <s>@Html.ActionLink(Model.Name, "Details", "Server", new { id = Model.Id }, null)</s>
    } else {
        @Html.ActionLink(Model.Name, "Details", "Server", new { id = Model.Id }, null)
    }
}