@model string
@{
    if (Model != null && !string.IsNullOrEmpty(Model)) {
        string[] lines = Model.Split('\n');
    
        foreach (string line in lines) {
            <p>@line</p>
        }
    }
}